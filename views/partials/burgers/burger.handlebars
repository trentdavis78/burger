<ol>
    <div class="row">
    <span class="col-1">{{id}}</span>
    <span class="col-3">{{burger_name}}</span>
      {{#unless devoured}}
        <span class="col-3"><button class="devour-it" data-id="{{id}}"> Devour It!</button></span> 
      {{/unless}}
      </div>
</ol>